# Repository-scoped working memory

Notes that persist with this repository: build commands, conventions, and facts
the agent has verified about this codebase. Advisory, not canonical.

Example (synthetic):

> **Running the store** — Put `src/` on `PYTHONPATH`, then
> `governed-memory verify` is the gate. Never merge with a failing verifier.
